# خطة تصميم محتوى الصفحات — Deken

**الإصدار:** 1.2  
**التاريخ:** 2026-04-24  
**سجل مختصر:** 1.1 — لوحة التحكم. 1.2 — نقطة البيع (`PosPage` + `pos.*` + حوار الدين + تنسيق عملات تجريبي).

**المرجع:** يفصّل [المرحلة 3 في خارطة 02](./02-implementation-roadmap.md) على مستوى **كل مسار**؛ التنفيذ في الكود بدون منطق قاعدة بيانات حتى إشعار آخر.

## الهدف

لكل صفحة رئيسية: **تخطيط محتوى واضح**، **مكوّنات UI**، **نصوص i18n (ar + en)**، **حالات فارغة/تحميل**، و**بيانات وهمية** حيث يلزم — ثم استبدال `PlaceholderPage` بصفحة مخصّصة عند اكتمال تصميم المحتوى لتلك الصفحة.

## كيفية التتبع

- عند إنهاء تصميم محتوى صفحة: ضع **`[x]`** على كل بند تابع لها في هذا الملف، وارفع التغيير مع Git.
- مفاتيح الترجمة: **`dashboard.*`** و**`pos.*`** جاهزة؛ للصفحات التالية استخدم **`products.*`**, **`debts.*`**, … بنفس الأسلوب (تجنّب تكرار نصوص طويلة تحت `pages.*` إلا للـ placeholders المؤقتة).

## مبادئ مشتركة (قبل إغلاق أي صفحة)

- [x] لا نصوص «hard-coded» في JSX — كل ما يظهر للمستخدم عبر i18n.
- [x] التخطيط يستخدم خصائص منطقية (RTL/LTR) ولا ينكسر عند تبديل اللغة.
- [x] الألوان والمسافات من **tokens** في `index.css` / CSS الصفحة حيث يناسب.

---

## 1 — لوحة التحكم (`#/dashboard`)

- [x] تقسيم الصفحة إلى أقسام (مثلاً: ملخص اليوم، اختصارات، تنبيهات لاحقة).
- [x] مكوّنات placeholder (بطاقات، أرقام وهمية أو شرطات).
- [x] مفاتيح `ar` / `en` لكل عنوان ووصف وفقات (`dashboard.*`).
- [x] حالة **empty** واضحة إن لم تُعرض بيانات لاحقاً (قسم التنبيهات).
- [x] استبدال المسار في `App.tsx` بصفحة `DashboardPage` عند الجاهزية.

**الكود:** `src/renderer/src/pages/dashboard/DashboardPage.tsx` + `DashboardPage.css`.

---

## 2 — نقطة البيع (`#/pos`)

- [x] هيكل شاشة البيع (سلة، إدخال باركود/بحث، ملخص دفع، أزرار إجراءات).
- [x] حقول وتركيز لوحة مفاتيح واضح للمسار السريع لاحقاً (تركيز تلقائي على حقل الإدخال بعد التحميل، Enter يضيف سطراً).
- [x] i18n لكل تسمية زر/عمود؛ تعدد عملات (ل.ل + USD تقريبي) كـ placeholder مع سعر صرف تجريبي موثّق في النص.
- [x] حالات **empty cart** / **loading** بصرياً (هيكل تحميل + سلة فارغة).
- [x] استبدال المسار بصفحة `PosPage` عند الجاهزية.

**الكود:** `src/renderer/src/pages/pos/PosPage.tsx`, `PosPage.css`, `DebtRecordDialog.tsx`, `formatPos.ts` — حوار **`<dialog>`** لتسجيل البيع كدين مع اسم زبون (جديد) وهاتف اختياري؛ إشعار نجاح (toast) بدون حفظ فعلي في DB.

---

## 3 — المنتجات والمخزون (`#/products`)

- [ ] جدول أو شبكة منتجات (رؤوس أعمدة، صفوف وهمية).
- [ ] شريط بحث/تصفية placeholder.
- [ ] i18n للجدول والأزرار (إضافة، تعديل — يمكن معطّلة بصرياً).
- [ ] **empty state** عند لا نتائج.
- [ ] استبدال المسار بصفحة `ProductsPage` عند الجاهزية.

---

## 4 — الديون (`#/debts`)

- [ ] قائمة عملاء أو دفتر ديون (صفوف وهمية + رصيد).
- [ ] تفاصيل جانبية أو صف ثانوي إن رُسمت في التصميم.
- [ ] i18n لكل عمود وإجراء.
- [ ] **empty state** لعدم وجود ديون/عملاء.
- [ ] استبدال المسار بصفحة `DebtsPage` عند الجاهزية.

---

## 5 — التقارير (`#/reports`)

- [ ] اختيار فترة placeholder + منطقة رسم/جدول وهمي.
- [ ] i18n للفلاتر والعناوين.
- [ ] **empty state** قبل وجود بيانات حقيقية.
- [ ] استبدال المسار بصفحة `ReportsPage` عند الجاهزية.

---

## 6 — الإعدادات (`#/settings`)

- [ ] مجموعات إعدادات (لغة، طباعة، عام — حسب الأولوية لاحقاً).
- [ ] عناصر تحكم placeholder (تبديلات، قوائم) بدون ربط تخزين بعد.
- [ ] i18n لكل مجموعة وحقل.
- [ ] استبدال المسار بصفحة `SettingsPage` عند الجاهزية.

---

## روابط

- [خارطة التنفيذ 02](./02-implementation-roadmap.md)
- [قوائم التحقق 02](./02-implementation-checklists.md)
- [فهرس الخطط](./README.md)
