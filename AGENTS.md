# Deken — توجيه الوكيل (Cursor)

هذا الملف يضمن أن **كل جلسة جديدة** في Cursor تبدأ من **نفس المسار**: قراءة موحّدة، مجلدات ثابتة، دون اختراع هيكل عشوائي.

## ترتيب القراءة الموصى به

1. **هذا الملف** (`AGENTS.md` في جذر المستودع).
2. **كل قواعد المشروع للوكيل** داخل **`.cursor/rules/*.mdc` فقط** — لا تُعتمد قواعد مبعثرة في مجلدات أخرى كمصدر رئيسي.
3. **الرؤية والخطة**: `docs/plans/01-master-plan.md`.
4. **أين يوضع كل نوع ملف**: `docs/STRUCTURE.md` + قاعدة `deken-repository-layout.mdc` ضمن `.cursor/rules/`.

## ثوابت (غير قابلة للمساومة)

- **تنظيم المجلدات**: كل نوع محتوى له مجلده (الخطط → `docs/plans/`، قرارات معمارية → `docs/architecture/`، إلخ). لا تُنشأ مجلدات جديدة في جذر المستودع دون تحديث `docs/STRUCTURE.md` (أو توثيق مكافٍ) في نفس التغيير.
- **قواعد Cursor**: ملفات `.mdc` **تبقى** تحت `.cursor/rules/` — هذا هو المكان الرسمي الوحيد الذي يحمّل Cursor القواعد منه بالشكل المتوقع.
- **أفضل الممارسات**: تغييرات صغيرة ومركزة؛ نصوص الواجهة عبر i18n؛ ترحيلات DB واضحة ومنطقية؛ عند إضافة مستند مهم حدّث الفهارس (`docs/README.md` و`docs/plans/README.md` حسب النوع).

## خريطة مختصرة للمستودع

| المسار | الدور |
|--------|--------|
| `docs/` | كل توثيق Markdown المنظم؛ التفاصيل في `docs/STRUCTURE.md` |
| `docs/plans/` | خطط المنتج والمراحل (مع فهرس `README.md` داخل المجلد) |
| `.cursor/rules/` | قواعد الوكيل (`.mdc`) — دائماً هنا |

هيكل كود التطبيق (عند وجوده) يُوضَّح في `deken-repository-layout.mdc`.
