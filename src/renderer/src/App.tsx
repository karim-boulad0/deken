import './App.css'

function App() {
  return (
    <main className="app">
      <h1 className="app__title">Deken</h1>
      <p className="app__subtitle">Electron + Vite + React — bootstrap ready.</p>
    </main>
  )
}

export default App
